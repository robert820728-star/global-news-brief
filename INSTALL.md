# 每日新聞安裝指引

本文件供 ChatGPT 在使用者貼上本 GitHub repo 網址後執行初始化。不得把本文件中的安裝流程文字放進每日新聞讀者版。

## 安裝目標

1. 建立或確認一個名為「每日新聞」的 ChatGPT 專案。
2. 在專案中保存使用者自己的地區、主題、篇幅、語言、時區與執行時間偏好。
3. 建立每日獨立排程；每次執行建立新對話，建議標題為 `YYYY/MM/DD 每日新聞`。
4. 每次執行先讀取本 repo 最新版 `news-brief-settings.md`、`news-brief-template.md`、`news-brief-examples.md`，再套用使用者個人偏好。
5. 公共 GitHub 規則與個人偏好分離；個人調整不得回寫公共 repo，除非 repo 擁有者明確要求。

## 使用者啟動指令

使用者可在新對話貼上 repo 網址並輸入：

> 請讀取此 GitHub repo 的 INSTALL.md，依序協助我建立「每日新聞」專案、個人偏好與每日獨立排程。任何建立專案、排程或授權動作都先取得我的確認。

## 初始化流程

### 一、驗證來源

- 確認 repo 可讀取。
- 確認三個核心文件與個人偏好範本存在。
- 若文件缺失或格式衝突，先回報，不建立不完整排程。

### 二、專案

- 檢查使用者是否已有「每日新聞」專案。
- 若沒有，請求授權後建立；若目前介面不能直接建立，提供最短人工建立步驟並在完成後繼續。
- 若已有同名專案，先詢問要沿用、另建副本或改名，不得建立重複專案。

### 三、個人偏好

至少詢問：

1. 主要居住國家或地區與城市。
2. 要獨立追蹤的國家、地區、洲別或全球板塊；數量不限。
3. 各板塊順序、權重、每日篇數上限與最低收納等級。
4. 特別提高或降低權重的主題。
5. 輸出語言。
6. 時區與每日執行時間。

台灣與中國預設獨立，不得自行合併。只有使用者明確要求時，才能建立「兩岸」「大中華」「東亞」等合併板塊。

### 四、板塊代碼

- 每個板塊使用唯一的三個大寫英文字母。
- 國家優先使用 ISO 3166-1 alpha-3：`TWN`、`CHN`、`JPN`、`KOR`、`USA`、`AUS`。
- 區域可使用：歐洲 `EUR`、東南亞 `SEA`、中東 `MEA`、非洲 `AFR`、全球／跨國 `GLB`。
- 建立排程前檢查格式與重複代碼。
- 事件編號格式：`XXX-01`、`XXX-02`。

### 五、排程

在使用者確認後建立獨立每日排程：

- 目的地：每日新聞專案。
- 頻率：每天一次。
- 時區與時間：依個人偏好。
- 每次執行：建立新的排程結果對話，不回到同一條長對話。
- 建議對話標題：`YYYY/MM/DD 每日新聞`。
- 新聞時間窗：以每次實際執行時間往前精確 24 小時。
- 規則來源：每次重新讀取 GitHub 最新版核心文件。
- 個人偏好優先於 repo 的預設地區與主題權重，但不得覆寫來源驗證、圖片驗收、地圖定位、繁體中文及安全規則。

### 六、首次測試

- 排程建立後，先手動執行一次。
- 檢查板塊數量與順序、三碼事件編號、來源、地圖、圖片、繁體中文、時間格式與三個頂層區塊。
- 測試成功後才視為安裝完成。

## 分享方式

### 分享 GitHub

接收者在自己的新對話貼上 repo 網址並執行「使用者啟動指令」。每個人自行授權建立專案與排程，並保存自己的個人偏好。

### 分享 ChatGPT 專案

接收者可加入共享專案作為參考，再依本安裝流程建立自己的專案與排程。共享專案或聊天副本不保證連帶複製排程，因此排程仍須由接收者自行確認建立。
