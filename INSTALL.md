# 每日新聞安裝指引

本文件供 ChatGPT 在使用者於新對話貼上本 GitHub repo 網址後執行初始化。不得把本文件中的安裝流程文字放進每日新聞讀者版。

## 安裝目標

1. 引導使用者在 ChatGPT 介面建立或確認名為「每日新聞」的專案。
2. 以最少問題建立使用者個人偏好。
3. 建立每日獨立排程；每次執行產生一個新的結果對話，不累積在同一條長對話。
4. 每次執行重新讀取本 repo 最新規則及使用者個人偏好。
5. 首次安裝後立即執行一次測試並驗收。

## 使用者啟動指令

使用者可在全新對話貼上 repo 網址並輸入：

> 請讀取此 GitHub repo 的 INSTALL.md，依序協助我建立「每日新聞」專案、個人偏好與每日獨立排程。任何建立專案、排程或授權動作都先取得我的確認。

收到指令後直接開始安裝，不要求使用者先理解 YAML、ISO 代碼、排程語法或地圖資料格式。

## 一、驗證來源

- 確認 repo 可讀取。
- 確認 `news-brief-settings.md`、`news-brief-template.md`、`news-brief-examples.md`、`user-preferences.example.yaml` 與 `daily-schedule-prompt.md` 存在。
- 若核心文件缺失或互相衝突，先回報，不建立不完整排程。

## 二、引導建立或確認專案

ChatGPT 不得嘗試自動建立專案，也不得把「建立專案」交給獨立排程測試。

- 必須在使用者貼上 repo 網址的原安裝對話中，先詢問是否已有「每日新聞」專案。
- 若沒有，提供最短人工步驟：
  1. 開啟 ChatGPT 側邊欄。
  2. 選擇「新增專案」。
  3. 專案名稱輸入「每日新聞」。
  4. 記憶選擇「僅限專案」或介面中的同等選項，讓專案內聊天可以共用脈絡，但不與專案外聊天互通。
  5. 完成建立後，回到原安裝對話回覆「已建立」。
- 若已有同名專案，請使用者回覆「沿用既有專案」。
- 在使用者明確回覆「已建立」或「沿用既有專案」前，必須停留在原安裝對話等待；不得建立正式排程、測試排程、獨立結果對話或失敗報告。
- 不得把安裝流程移到「排程」頁面，也不得要求使用者自行尋找結果。
- 使用者確認專案後，才繼續詢問三項偏好及建立排程。
- 專案建立後，將本 repo 網址、核心文件及個人偏好視為該專案的長期來源。
- 建議使用「僅限專案記憶」：板塊、主題權重、語言、時區與排版偏好可在專案內沿用；專案外聊天不得影響每日新聞。
- 記憶只能保存偏好與工作方式，不能把前一天的新聞內容當成今天的事實。每次執行仍須重新搜尋精確 24 小時時間窗，並重新驗證來源、數字、時間、地圖與圖片。
- 若使用者選擇其他記憶模式，可以繼續安裝，但應簡短提醒可能混入專案外偏好的風險，不得強迫更改。

### 正確流程

使用者貼上 repo 網址 → 原安裝對話引導建立專案 → 使用者回覆「已建立」 → 詢問偏好 → 取得排程授權 → 建立排程 → 執行首次測試。

### 錯誤流程

在專案尚未建立或確認時，先建立五分鐘後測試排程，再把「無法建立專案」的結果送到「排程」中的陌生獨立對話。此流程禁止使用。

## 三、只詢問三件事

### 問題一：監控板塊

> 是否要自訂新聞監控板塊？板塊可以是單一國家，也可以是區域，例如台灣、中國、日本、美國、歐盟、北美、非洲或東南亞。若不自訂，使用預設的「台灣、中國、世界」。

- 板塊數量不限。
- 台灣與中國預設獨立；只有使用者明確要求才可合併。
- 使用者只需提供自然語言名稱；國家、區域、組織、成員範圍與三碼代碼由後台解析。
- 只有名稱確實有歧義時才追問範圍，不要求使用者填寫成員國、ISO 代碼或地理分類。

### 問題二：主題偏好

> 是否要提高或降低某些新聞主題的權重？若沒有，使用預設設定。

- 只記錄使用者明確提出的差異。
- 未指定項目沿用 repo 預設。
- 個人偏好不得降低來源驗證、圖片驗收、地圖定位與安全規則。

### 問題三：執行時間

> 希望每天幾點產生新聞簡報？

- 時區優先使用使用者帳號、裝置或目前工作區時區。
- 只有時區無法判斷、跨時區使用或使用者主動指定時才追問。
- 不要求使用者填寫 RRULE 或其他排程語法。

## 四、自動判斷，不例行詢問

### 語言

1. 優先使用使用者已明確設定的輸出語言。
2. 沒有設定時，使用安裝對話的主要語言。
3. 對話混用多種語言時，以使用者主要使用語言為準。
4. 只有無法可靠判斷或使用者要求變更時才詢問。
5. 語言套用於板塊名稱、欄位、標題、內文、地圖圖說與圖片圖說；來源原名及網址可保留。

### 板塊代碼

- 每個板塊配置唯一的三個大寫英文字母。
- 國家優先使用 ISO 3166-1 alpha-3；區域或組織使用清楚、穩定且不衝突的三碼。
- 事件編號格式固定為 `XXX-01`、`XXX-02`。
- 建立排程前自動檢查代碼格式與衝突；只有無法合理消解時才詢問使用者。

### 篇幅與門檻

- 未指定時沿用 `user-preferences.example.yaml` 的預設值。
- 板塊增加時不得為了填滿每區而加入低價值新聞。
- 同一事件只放在一個主要板塊，跨國系統性事件才優先放全球板塊。

## 五、建立每日獨立排程

只有在使用者已明確確認「每日新聞」專案存在，且完成偏好與排程授權後，才可依 [daily-schedule-prompt.md](daily-schedule-prompt.md) 建立排程：

- 名稱：每日新聞。
- 目的地：使用者的「每日新聞」專案。
- 頻率：每天一次。
- 執行時間與時區：依個人偏好。
- 模式：獨立排程；每次執行建立新的結果對話，不返回安裝對話，也不持續堆疊於同一對話。
- 建議結果對話標題：`YYYY/MM/DD 每日新聞`。
- 新聞時間窗：每次實際執行時間往前精確 24 小時。
- 規則來源：每次重新讀取 GitHub 最新版核心文件。
- 個人偏好：保存在使用者自己的專案或排程設定中，不回寫公共 repo。

專案由使用者依上述介面步驟建立；排程由 ChatGPT 在取得授權後建立。專案存在是排程建立的必要前置條件，不得跳過、假設或用測試排程代替驗收。

## 六、首次測試

- 排程建立後立即手動執行一次，不必等待隔日。
- 檢查：
  - 結果是否出現在「每日新聞」專案的新對話，而非安裝對話。
  - 對話標題是否接近 `YYYY/MM/DD 每日新聞`。
  - 板塊數量、順序與三碼事件編號是否符合偏好。
  - 語言、時區、24 小時時間窗、來源、地圖、圖片與三個頂層區塊是否正確。
- 若測試失敗，只修正失敗環節，不重新詢問已確認的偏好。
- 測試成功後回報專案名稱、每日執行時間、監控板塊與排程狀態。

## 分享方式

### 分享 GitHub

接收者在自己的新對話貼上 repo 網址並執行啟動指令。每個人各自授權建立專案與排程，並保存自己的偏好。

### 分享 ChatGPT 專案

接收者可加入共享專案作為範例，再依本流程建立自己的專案與排程。共享專案或聊天副本不保證連帶複製排程，因此排程仍由接收者確認建立。
