# 新聞簡報設定

## 目的

用版本化文字檔保存使用者的每日新聞簡報規則，讓後續修改可以被檢查、追蹤與還原。

## 排程

- 正式每日新聞簡報：除非使用者明確要求，否則保留既有正式排程，不任意修改。
- 獨立測試：使用者要求測試時，建立獨立任務；不得合併、暫停、修改或干擾既有新聞任務。
- 時間範圍：每次執行時，以實際執行時間往回推 24 小時判斷新聞。

## 模組化流程

新聞簡報必須以「事件處理流程」執行，不是用單一自由寫稿提示一次生成。搜尋階段不得直接開始撰寫最終簡報。
- 輸出格式必須先套用 `news-brief-template.md`。該模板定義讀者版骨架、總覽表格欄位、逐條詳報欄位順序、欄位空行、圖片處理與最終格式檢查；若本設定檔與模板都提到輸出格式，先以模板作為版面骨架，再套用本設定檔的篩選與來源規則。
- 可參考 `news-brief-examples.md` 的正確與錯誤格式範例。該檔只用於版面穩定與格式驗收，不得照抄範例事件內容，也不得把範例事件視為本次新聞。

## 核心輸出規則

- 最終輸出必須只有三個頂層區塊，且順序固定：`今日總覽`、`逐條詳報`、`後續觀察`。
- `今日總覽` 必須分成 `台灣版`、`中國版`、`國際版` 三個小節，每個小節使用 Markdown 表格。
- 總覽表格固定欄位為：`編號 | 時間 | 事件 | 等級`。
- 事件編號使用 `T1/T2`、`C1/C2`、`I1/I2`，且逐條詳報必須沿用相同編號。
- `逐條詳報` 每則標題必須使用 `### T1. 事件名稱 - A`，不得使用 `標題：T1...`。
- `逐條詳報` 欄位順序固定為：標題、時間、來源、圖片、事件細節、各方說法、分析。
- 欄位名稱與欄位內容必須同一段，例如 `**時間：**8/12。`、`**來源：**[路透社](https://example.com)。`
- 不同欄位之間必須空一行；不得把欄位名稱與內容拆成兩段。
- `各方說法` 只有在存在明顯不同立場、資料差異或利害關係人角度時才列，沒有就省略。
- 所有公開欄位必須使用繁體中文，不得顯示英文欄位名或事件分析卡內部欄位。
- 事件名稱、段落內文、圖片說明與各方說法中的外文專有名詞，若已有通行繁體中文譯名，必須優先使用繁體中文譯名；必要時可在第一次出現時以括號保留原文輔助辨識，但不得讓英文成為主標題或主要事件名稱。
- 例如颱風名稱、地名、機構名、政策名、疾病名、科技名詞若有可靠中文譯名，須寫成繁體中文；不得在標題或總覽中直接寫 `Dolphin` 這類未翻譯英文。

## 圖片硬規則

圖片欄必須比照內文欄位使用固定格式，且位置固定在 `來源` 後、`事件細節` 前。

### B 以上事件

- B 以上事件，只要本則引用來源本身有可信照片、戰爭畫面、災害現場圖、颱風路徑圖、衛星圖、官方圖表、資料視覺化或新聞頁首圖，就必須強制嘗試下載或截圖。
- 圖片來源必須與本則 `來源` 欄一致；不得為了補圖另找未引用來源。
- 優先順序固定為：官方圖表/地圖/路徑圖，其次為通訊社或主流媒體新聞頁內照片，其次為來源頁面圖片區塊截圖。
- 災害、颱風、地震、火山、洪水、疫情、戰爭與大規模公共安全事件，若來源有地圖、路徑圖、震央圖、衛星圖、警戒區域圖或官方統計圖，圖一必須優先放這類「幫助定位與理解事件範圍」的資訊圖；現場照片可放圖二或後續圖片。
- 颱風事件的圖一優先使用官方颱風路徑圖、地面天氣圖、衛星雲圖或警戒區域圖；地震事件的圖一優先使用官方震央位置圖、烈度圖或影響範圍圖。
- 圖片必須轉成可直接顯示的附件或本地圖片，使用 `sandbox:/absolute/path/to/image.png` 這類可顯示路徑。
- 不得只貼短網址、追蹤跳轉網址、裸 CDN 圖片連結或需要反覆跳轉載入的圖片網址。
- 若引用來源頁面有圖但下載失敗，必須改試截圖；若截圖也失敗，才可省略圖片欄。
- 若引用來源本身沒有圖片，或圖片無法穩定取得，省略圖片欄，不要寫「無圖片」或「圖片失敗」。
- 圖片處理不得影響事件等級；不能因為圖片震撼、漂亮或容易傳播而提高等級。

### C 級與 C- 事件

- C 級與 C- 事件不強制圖片。
- 若圖片本身能明顯幫助理解，例如地圖、政策圖卡、產業流程圖、官方統計圖，可依同樣格式加入。

### 圖片欄固定格式

```markdown
**圖片：**本則 B 以上事件的引用來源提供可信圖片，已下載或截圖為可顯示附件。

![圖一：圖片說明](sandbox:/absolute/path/to/image-1.png)

圖一說明：圖片內容，拍攝/發布/截圖時間（來源：[來源名稱](https://example.com)）。

![圖二：圖片說明](sandbox:/absolute/path/to/image-2.png)

圖二說明：圖片內容，拍攝/發布/截圖時間（來源：[來源名稱](https://example.com)）。
```

### 圖片欄排版規則

- 多張圖片必須直向排列，不得重疊、不得合成圖廊、不得使用會來回載入的多圖小卡。
- 多張圖片固定使用以下順序：圖一圖片、圖一說明（圖一來源）、圖二圖片、圖二說明（圖二來源）、圖三圖片、圖三說明（圖三來源）。
- 圖片說明必須標明圖片內容、拍攝/發布/截圖時間（若來源提供）、來源名稱與來源連結。


### 事件分隔規則

- `逐條詳報` 中每則事件之間必須加入水平分隔線 `---`。
- 分隔線位置固定：上一則事件最後一個欄位結束後空一行，接著一行 `---`，再空一行，才開始下一則 `### 編號. 事件名稱 - 等級`。
- 第一則事件標題前不得加分隔線；最後一則事件結尾後不得加多餘分隔線。

## 候選海選

- 廣泛搜尋過去 24 小時內的候選事件，範圍包含國際、台灣、中國、金融、科技/資安、文化/媒體、災害、公衛、科學/太空等類別。
- 海選階段只收集候選事件，不寫最終段落。
- 候選數量上限只允許作為防止搜尋爆量的操作限制，不得變成最終入選目標或固定篇數。
- 每個原始候選只保留精簡工作欄位：暫定標題、來源連結、區域/影響範圍、類別、時間，以及一句「可能重要的原因」。

## 事件去重

- 排名之前，先合併描述同一底層事件的多篇報導。
- 不同標題、股票代碼、翻譯、來源語言、後續角度，只要描述同一發展，就視為同一事件。
- 每個合併後事件保留最強或最接近原始資訊的來源。
- 不得因為某事件報導很多，就擠掉較少報導但獨立且值得知道的事件。

## 入選門檻

- 每個去重後事件都必須先通過使用者的「值得知道」標準，再進入寫稿。
- 只有具備明確重要性的事件才入選，例如公共安全、治理、地緣政治、國家或區域相關性、重大公司或市場影響、產業結構、科技/資安含義、文化意義，或能揭示社會趨勢的訊號。
- 不得只因為新聞很新、很多媒體報導、情緒很大、或剛好搜得到就入選。
- 只要明確通過標準，就必須納入，即使當天簡報變長。
- 弱事件必須刪除，即使當天簡報變短。

## 來源驗證與交叉確認

- 入選事件必須優先追溯原始來源，例如官方公告、監管文件、公司公告、研究機構原文、政府資料、路透、彭博、金融時報、美聯社等主要通訊社或一手媒體。
- 中文媒體轉述外媒時，不得只列中文轉述來源；應盡量同時列出外媒原始報導或更接近原始資訊的來源。
- `A-` 以上事件原則上至少需要兩個來源，且其中至少一個應為原始來源、官方來源或主要國際/本地權威媒體。
- `B` 事件若牽涉重大金額、上市公司控制權、公共安全、災害、政策或市場影響，原則上也應交叉確認至少兩個來源。
- 若事件重要但目前只能找到單一可信來源，可以入選，但必須在 `來源` 或 `分析` 中明確說明「目前主要依據單一來源」，並降低語氣確定性。
- 不得讓單一轉述來源支撐高嚴重度判斷。若原始來源不可得，應把不確定性寫入事件分析卡與讀者版分析。
- 來源數量不是湊數；同一通訊社被多家網站轉載，仍視為同一來源脈絡。應優先增加獨立來源，而不是列出多個轉載頁面。

## 嚴重度與收納標準

- `SS`：極端全球性或系統性危機風險，例如多場重大戰爭合流、數個大國被捲入，或短期內具可信世界大戰風險。
- `S`：嚴重國際或區域危機，或重大結構轉折，具備真實可能性改變後續歷史、研究、產業、政策、安全或長期發展。
- `A`：必須注意的重大事件，例如嚴重戰爭升級、多國疾病擴散、極高傷亡、重大災害，或重要政治/經濟衝擊。
- `B`：重要但影響仍有限的事件，例如尚未造成廣泛區域崩潰的新興疾病、有意義但受限的衝突升級，或重要政策/經濟事件。
- `C`：常規或低訊號事件。若事件具備文化、產業、平台、創作者經濟、網文、遊戲或電競制度意義，可以列入短報。
- `C-`：低權重但有明確產業或公共訊號的短報。

## 產業型文化與娛樂降權規則

- 普通藝人八卦、戀情、粉圈衝突、宣傳活動、新劇新綜藝、票房或收視率，原則上不列入。
- 若事件超出娛樂新聞，能反映平台治理、創作者收入、產業制度、勞動壓力、法規、審查、資金模型或公共文化影響，可以低權重納入。
- 產業型文化、平台、網文、遊戲或電競事件，即使使用者個人未必感興趣，只要影響整個產業、平台生態、創作者收入模式、內容分發規則、法規環境、國際賽事制度或大量從業者，仍可納入低權重事件。
- 例如走鐘獎停辦、起點收費機制改革、主要平台分潤或推薦機制調整、電競遊戲法規變化、國際賽事制度改革等，可列為 `C` 或 `C-`；若影響範圍更大、涉及監管或商業模式重大改變，可升至低 `B`。
- 這類事件必須短報，重點放在產業結構、平台規則與後續影響，不要寫成粉絲向、宣傳稿或流量新聞。

## 事件分析卡

每個入選事件在最終輸出前，必須先建立後台事件分析卡：

1. `事件名稱`
2. `嚴重度`
3. `影響範圍`
4. `來源`
5. `圖片`，B 以上事件必須記錄是否已嘗試下載/截圖，以及是否成功取得可顯示附件
6. `事件時間`
7. `為何入選`
8. `事件細節`
9. `各方說法`，只在有用時保留
10. `分析判斷`
11. `升級/降級條件`

事件分析卡是後台規劃結構。最終回答必須使用公開輸出格式，不得把未整理的工作筆記顯示給使用者。

## 品質檢查

回覆簡報前，必須檢查：

- 三個頂層區塊存在，且順序正確。
- 已依 `news-brief-template.md` 完成版面檢查；今日總覽表格、逐條詳報欄位、圖片欄與空行規則皆符合模板。
- `今日總覽` 易讀，且明確分成 `台灣版`、`中國版`、`國際版` 三個小節；每個小節使用緊湊表格。
- 每個事件都有事件號碼、事件時間、事件名稱與嚴重度。
- 重要的 `B` 與 `C` 事件沒有因為高嚴重度事件存在而被漏掉。
- 沒有為了平衡分類或湊數量加入填充新聞。
- 重複報導已合併成事件，而不是拆成多個條目重複出現。
- `A-` 以上事件已盡量交叉確認至少兩個來源；若只能使用單一可信來源，讀者版已清楚標示來源限制。
- B 以上事件若引用來源有圖，已強制嘗試下載或截圖；只有來源無圖或下載/截圖皆失敗時才省略圖片欄。
- `逐條詳報` 每則新聞已使用 `**時間：**內容` 這種欄位名與內容同段格式；不同欄位之間有一個空行，且沒有把欄位名與內容拆成兩段。
- 若有圖片欄，已使用下載或截圖附件優先的直向排列格式，並逐張附說明與來源。
- 最終讀者版不得出現英文欄位名；公開欄位、事件標題與圖片說明必須使用繁體中文，已有通行譯名的外文專有名詞不得直接裸露。
- `逐條詳報` 的事件之間已用 `---` 分隔線清楚切開。
- 災害、颱風、地震、疫情、戰爭與公共安全事件若有地圖、路徑圖、震央圖、衛星圖或官方圖表，圖一已優先使用資訊圖，而不是只放現場照片。

## 目前確認狀態

- 新聞簡報格式已更新為包含：標題、時間、來源、圖片（B 以上強制嘗試）、事件細節、各方說法、分析。
- `今日總覽` 已更新為三個小節各自使用表格，表格欄位固定為：編號、時間、事件、等級。
- `逐條詳報` 已更新為包含時間欄位，欄位順序為：標題、時間、來源、圖片（B 以上強制嘗試）、事件細節、各方說法、分析。
- `圖片` 欄位必須比照內文使用固定格式，欄位名與內容同段，圖片與說明直向排列；B 以上事件若引用來源有圖，必須先下載，失敗則截圖，只有來源無圖或下載/截圖皆失敗時才省略。
- `各方說法` 無用時應省略。
- 每次簡報應先以緊湊總覽表格開頭，統計值得注意項目，並在表格 `等級` 欄標示 `SS/S/A/B/C`。
- `B/C` 項目應使用與 `A/S` 相同的核心格式。`B` 是獨立新聞條目；`C` 只有在確實輕微且相關時才可合併。
- `C` 項目在有用時可使用約 50-100 個中文字，讓脈絡與意義清楚，即使它仍屬低嚴重度。
- 曾要求建立五分鐘後的獨立測試任務，且該任務應與正式每日新聞任務分開。
- 最終讀者版所有公開欄位必須使用繁體中文，不得顯示英文欄位名。
