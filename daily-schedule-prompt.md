# 每日新聞獨立排程提示詞

本文件是排程執行規格，不是讀者版內容。

## 對話與日期硬規則

- 排程名稱固定為「每日新聞」。
- 每次執行使用獨立結果對話，不回到安裝對話或前一天的新聞對話。
- 可控制結果對話名稱時，固定命名為「每日新聞」，不得改成英文名稱、隨機摘要或其他名稱。
- 每份讀者版第一行必須顯示執行地時區日期，格式固定為 `YYYY/MM/DD 每日新聞`。
- 日期必須依每次實際執行時間重新計算，不得沿用前一次日期。
- ChatGPT 專案不是安裝前提；不得嘗試建立、尋找、指定、驗收或宣稱已歸入任何專案。
- 不得在讀者版加入專案歸屬、對話位置、安裝狀態或後台限制說明。

每次排程執行時：

1. 讀取 GitHub repo `robert820728-star/global-news-brief` 最新版：
   - `news-brief-settings.md`
   - `news-brief-template.md`
   - `news-brief-examples.md`
   - `user-preferences.example.yaml`
2. 讀取排程中保存的使用者個人偏好；個人偏好只覆寫監控板塊、順序、權重、篇數、最低等級、主題、語言、時區與執行時間，不得覆寫來源驗證、圖片驗收、地圖定位、事件去重與安全規則。
3. 以實際執行時間往前精確 24 小時搜尋與篩選新聞。
4. 完成候選海選、事件去重、入選門檻、事件分析卡、來源交叉驗證、官方或原始來源回查、地圖與圖片品質檢查。
5. 依個人偏好動態產生所有新聞板塊。未設定偏好時使用台灣 `TWN`、中國 `CHN`、世界 `GLB`。
6. 事件編號使用板塊三碼與兩位流水號，例如 `TWN-01`、`CHN-01`、`GLB-01`。
7. 先輸出日期行 `YYYY/MM/DD 每日新聞`，再輸出讀者版。
8. 最終讀者版只保留 `今日總覽`、`逐條詳報`、`後續觀察` 三個頂層區塊。
9. 若本次沒有值得回報的事件，仍建立當日結果並保留正確日期，簡短說明時間窗內沒有事件通過門檻；不得以舊新聞補數量。
