# 新聞簡報輸出模板

本檔只定義 canonical 讀者版骨架與排版。不得把後台事件資料、執行狀態、驗收摘要或修復過程寫進讀者版。

## 固定骨架

讀者版固定以標題、manifest 衍生的統計期間及六項評級說明開頭，其後只能依序出現 `## 今日總覽`、`## 逐條詳報`、`## 後續觀察` 三個二級標題。

```markdown
# 每日新聞讀者版

統計期間：2026 年 8 月 19 日 13:14:07 至 8 月 20 日 13:14:07（台北時間）

評級綜合考量：重要性／嚴重程度、影響範圍、急迫與安全、結構／政策意義、本期實質新進展、核心板塊關聯。

## 今日總覽

## 逐條詳報

## 後續觀察
```

統計期間由 manifest 的 `run.window_start` 與 `run.window_end` 換算為使用者時區，不得手填或沿用前次日期。執行編號、commit、正式發布狀態、候選筆數、HTTP 狀態、驗收結果與手填總數摘要只留在內部 manifest／run log，不顯示於讀者版。

## 今日總覽

依使用者設定的板塊順序逐一輸出，每個有入選事件的板塊使用一張 Markdown 表格：

```markdown
### 台灣

| 編號 | 時間 | 事件 | 等級 |
|---|---|---|---|
| TWN-01 | 8/20 13:00 | 事件名稱 | A |
```

- `編號`：板塊三碼加兩至三位流水號，例如 `TWN-01`、`CHN-01`、`JPN-01`。
- `時間`：只放一個主要新聞時間、官方更新時間或統計截止時間，並先換算為使用者時區。
- `事件` 與 `等級`：必須和逐條詳報中的同一事件完全一致。
- 板塊數量由偏好決定，不得固定為台灣、中國、世界三區；沒有通過門檻的板塊可省略，不得用舊聞補數量。

## 逐條詳報

每則事件沿用今日總覽的編號、標題與等級，固定順序如下：

```markdown
### TWN-01. 事件名稱 - A

**時間：**新聞時間：8/20 13:00；事件時間：8/20 12:30；更新節點：8/20 官方更新。

**來源：**[官方或原始來源](https://example.com)、[獨立媒體](https://example.com)。

**地圖：**

![地圖一：事件位置](sandbox:/absolute/path/to/map.png)

地圖一：事件位置與影響區域，依來源資料整理。

**資料圖表：**

![資料圖表一：數值比較](sandbox:/absolute/path/to/chart.png)

資料圖表一：本簡報依官方資料製作；比較口徑與截止時間見圖說。

**圖片：**

![圖一：事件現場](sandbox:/absolute/path/to/image.jpg)

圖一：事件現場，拍攝時間 8/20（來源：[來源名稱](https://example.com)）。

**事件細節：**說明發生什麼、影響對象、規模、已知數字、背景、目前狀態與限制。跨日事件在此整理每日或每階段進展。

**各方說法：**只有官方、受影響者、專家、反對方或第三方存在實質差異時才保留；沒有差異就省略整個欄位。

**分析：**說明真正值得注意的核心、可信度、不確定性、公共價值、可能影響與具體後續追蹤條件。
```

### 欄位規則

- `時間／來源／事件細節／分析` 為每則必填；地圖、資料圖表、圖片與各方說法依事件資料條件式顯示。
- 時間、來源、地圖、資料圖表、圖片、事件細節、各方說法、分析之間各空一行；除附件與圖說外，欄位名稱與內容不得拆段。
- 來源必須是可開啟連結。若只有一個可靠來源，來源欄或分析欄必須包含：`目前僅找到一個可靠來源，尚無其他獨立來源交叉確認。`
- 每張可見附件必須與 manifest 完全一致，使用 Markdown 圖片附件，下一個非空白段落緊接其圖說；順序固定為地圖、資料圖表、來源圖片。
- 沒有實際可見附件時，省略對應的整個視覺欄位，不顯示圖片說明、caption、失敗理由或占位文字。internal `reader_omission_note` 只留在 evidence／receipt。
- `map.claim_critical=true` 時必須附上已驗收地圖；非主張關鍵的視覺取得失敗可在後台記為 omitted，不得在讀者版留下占位。
- 第一則事件前不加分隔線；兩則事件之間固定使用一條 `---`；最後一則事件後不加多餘分隔線。

## 後續觀察

只列真正需要追蹤的具體條件，沿用事件編號：

```markdown
## 後續觀察

- TWN-01：死亡達 100 人、撤離超過 2 萬人或主要道路恢復通行時，更新等級與救援進度。
```

每項必須與 manifest 的 `detail.follow_up` 逐字一致；沒有追蹤條件的事件不硬湊。禁止使用「持續關注後續發展」、「追蹤官方後續更新與實際影響」等通用占位句。

## 讀者版禁止內容

- `READER_INTERNAL_REPAIR_LOG_EXCLUSION_GATE`：任何修復紀錄、重試過程、HTTP 狀態、archive 切換、圖片補救或 checkpoint 重建不得出現在讀者版。
- 驗收摘要、執行編號、程式版本、正式發布、候選筆數、內部檔案位置、手填總數摘要或 Task 更新通知。
- 簡化的「分區標題 → `### 事件名稱｜A` → 摘要 → 評級評論」單項新聞版型；它不是發布路徑。
- 只有圖說沒有附件、只有附件沒有圖說、以網址代替附件，或以圖片說明冒充不存在的圖片。
- 搜尋結果頁、登入頁、錯誤頁、空白圖、未載入地圖或範例檔中的示範事件。

## 送出前驗證

完成草稿後執行：

```bash
python3 scripts/validate_news_brief.py brief \
  --manifest /path/to/news-event-manifest.json \
  --input /path/to/news-brief.md
```

驗證失敗時只重做 reader render，不得重跑 discovery、去重、評分、驗證或圖片收集。
