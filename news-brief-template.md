# 新聞簡報輸出模板

本檔只定義既有讀者版骨架與排版。不得把後台事件資料、執行狀態、驗收摘要或修復過程寫進讀者版。

## 固定骨架

讀者版固定以標題、統計期間及六項評級說明開頭，接著依設定順序逐區輸出「本區完整清單＋本區逐則新聞」。不得把所有清單移到前面，再另建欄位式逐條詳報。

```markdown
# 每日新聞讀者版

統計期間：2026 年 8 月 19 日 13:14:07 至 8 月 20 日 13:14:07（台北時間）

評級綜合考量：重要性／嚴重程度、影響範圍、急迫與安全、結構／政策意義、本期實質新進展、核心板塊關聯。

## 🇹🇼 台灣新聞

| 時間 | 事件 | 評級 |
|---|---|---:|
| 8/20 13:00 | 事件名稱 | A |

### 事件名稱｜A

![事件圖片](sandbox:/absolute/path/to/image.jpg)

事件摘要，交代發生什麼、影響對象、規模、已知數字、背景、目前狀態與限制。

評為A級，是因為具體的重要性／嚴重程度與影響範圍；同時交代可信度、不確定性及下一個實質觀察條件。[官方來源](https://example.com)、[獨立媒體](https://example.com)
```

統計期間由 manifest 的 `run.window_start` 與 `run.window_end` 換算為使用者時區，不得手填或沿用前次日期。執行編號、commit、正式發布狀態、候選筆數、HTTP 狀態與驗收結果只留在內部 manifest／run log，不顯示於讀者版。

## 板塊與清單

- 台灣、中國、世界預設分別使用 `## 🇹🇼 台灣新聞`、`## 🇨🇳 中國新聞`、`## 🌍 國際世界`；其他自訂板塊沿用設定名稱。
- 每個有入選事件的板塊先放一張 `時間｜事件｜評級` 三欄表格，列出該板塊全部入選事件；不得漏列、跨區混放或改成卡片。
- 清單時間只放一個主要新聞時間、官方更新時間或統計截止時間，並先換算為使用者時區；不得附加 UTC、GMT、數字偏移或時區名稱。
- 清單標題與評級必須和下方同一則新聞完全一致。沒有達門檻事件的板塊可省略，不得用舊聞補數量。

## 每則新聞固定格式

每則新聞固定使用下列順序，不顯示事件編號，也不得改成 `時間／來源／圖片／事件細節／分析` 欄位式報告：

1. `### 事件名稱｜A` 標題。
2. 合格圖片附件；沒有合格圖片時只顯示一行非技術性的 `**圖片說明：**`。
3. 一段新聞摘要。
4. 一段以 `評為A級`、`評為B+級` 等實際等級起首或明確包含該句式的評級評論，來源連結直接接在段末。
5. 與下一則新聞之間放 `---`；整份 reader 最後一則後不放多餘分隔線。

摘要與評級評論不得合併成難以閱讀的單一長段。來源必須是可開啟連結；官方或原始來源通常列前，媒體、通訊社、專家與受影響方來源列後。若只有一個可靠來源，評級評論須包含「目前僅找到一個可靠來源，尚無其他獨立來源交叉確認。」

## 圖片、地圖與資料圖表

- 每則新聞預設一張合格來源圖片；第二張必須提供第一張沒有的新資訊，最多兩張。
- 每張附件後緊接新聞式圖說；多張圖片直向排列，不使用圖廊、小卡或重疊排版。
- 有合格自製地圖或資料圖表時，放在來源圖片之前，並保留清楚圖說。地圖與資料圖表不計入來源圖片上限。
- `map.required=true` 時必須附上已驗收地圖；不得以 omitted 或純文字理由完成正式讀者版。
- reader 內的每一張圖片都必須逐一對應 manifest 且位於所屬單項新聞內；頁首、統計期間、板塊表格、新聞之間及結尾不得另放圖片。
- 沒有合格圖片時，省略空白圖片欄，改用一行 `**圖片說明：**` 顯示 manifest 的 `images.reader_omission_note`；不得暴露下載、截圖、重試或錯誤碼。
- 圖片必須是對話中真正可見的附件或原生媒體，不得用網址、Markdown 外連或空白占位符冒充。

## 讀者版禁止內容

- `READER_INTERNAL_REPAIR_LOG_EXCLUSION_GATE`：任何「修復紀錄」、重試過程、HTTP 狀態碼、archive 切換、效能調整、圖片補救或 checkpoint 重建都不得出現在讀者版。
- `驗收摘要`、執行編號、程式版本、正式發布、候選筆數、內部檔案位置或 Task 更新通知。
- `## 今日總覽`、`## 逐條詳報`、`## 後續觀察`，以及 `**時間：**`、`**來源：**`、`**事件細節：**`、`**分析：**` 等欄位式逐條詳報。
- 英文欄位名稱、事件分析卡、技能名稱、後台狀態或未翻譯的通行外文專有名詞。
- 搜尋結果頁、登入頁、錯誤頁、空白圖、未載入地圖或範例檔中的示範事件。

## 送出前驗證

完成草稿後執行：

```bash
python3 scripts/validate_news_brief.py brief \
  --manifest /path/to/news-event-manifest.json \
  --input /path/to/news-brief.md \
  --reader-layout legacy-sectioned
```

驗證失敗時只修正 reader render，不得重跑 discovery、去重、評分、驗證或圖片收集。
