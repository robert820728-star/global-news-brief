# 新聞簡報輸出模板

本檔只定義讀者版骨架與排版。選題、驗證、地圖與圖片的執行規則分別由 repo 內技能負責；不得把後台事件資料或技能執行狀態寫進讀者版。

## 固定骨架

最終輸出第一行為日期行，其後只能出現三個二級標題：

```markdown
YYYY/MM/DD 每日新聞

## 今日總覽

## 逐條詳報

## 後續觀察
```

不得加入前言、完成通知、執行狀態、安裝資訊、專案歸屬或第四個二級標題。

## 今日總覽

依使用者設定的板塊順序逐一輸出，每個板塊使用一張 Markdown 表格：

```markdown
### 台灣版

| 編號 | 時間 | 事件 | 等級 |
|---|---|---|---|
| TWN-01 | 8/14 09:30 | 事件名稱 | A |
| TWN-02 | 截至 8/14 | 事件名稱 | B |
```

### 欄位定義

- `編號`：板塊三碼加兩位流水號，例如 `TWN-01`、`CHN-01`、`GLB-01`。
- `時間`：只放一個主要新聞時間、官方更新時間或統計截止時間。
- `事件`：使用繁體中文的一句事件名稱，不在此欄加入摘要、來源或等級。
- `等級`：使用 `SS`、`S`、`A`、`B`、`C` 及其加減級。

板塊數量由偏好決定，不得固定為三個。沒有入選事件的板塊可以省略，或在表格中簡短標示沒有事件通過門檻，不得以舊新聞補數量。

## 逐條詳報

每則事件沿用今日總覽的編號、標題與等級：

```markdown
### TWN-01. 事件名稱 - A

**時間：**新聞時間：8/14 09:30；事件時間：8/14 08:00；更新節點：8/14 官方更新。

**來源：**[官方或原始來源](https://example.com)、[獨立媒體](https://example.com)。

**地圖：**

![地圖一：事件位置定位](sandbox:/absolute/path/to/map-1.png)

地圖一：事件位置與影響區域，依來源資料整理。

**圖片：**

![圖一：官方資訊圖](sandbox:/absolute/path/to/image-1.png)

圖一：官方統計或專業資訊圖，發布時間 8/14 08:00（來源：[來源名稱](https://example.com)）。

![圖二：新聞現場照片](sandbox:/absolute/path/to/image-2.jpg)

圖二：事件現場，拍攝時間 8/14（來源：[來源名稱](https://example.com)）。

**事件細節：**說明發生什麼、影響對象、規模、已知數字、背景、目前狀態與限制。跨日事件在此整理每日或每階段進展。

**各方說法：**只有存在官方、受影響者、專家、反對方或第三方的實質差異時才保留；沒有差異就省略整個欄位。

**分析：**說明真正值得注意的核心、可信度、不確定性、可能影響與後續追蹤條件。
```

### 時間欄

- 欄位名稱與內容必須同一段。
- 依序使用新聞時間、事件時間、期間、更新節點；不適用者省略。
- 疫情、颱風、地震、戰爭、救援等跨日進展放在事件細節，不把完整時間線擠進時間欄。

### 來源欄

- 所有實際使用的來源都要有可開啟連結。
- 官方或原始來源通常列前，媒體、通訊社、專家與受影響方來源列後；實際排序仍依事件證據作用調整。
- 若搜尋後只有一個可靠來源，來源欄或分析欄必須包含：`目前僅找到一個可靠來源，尚無其他獨立來源交叉確認。`
- 單一來源說明不改變事件原有等級，也不代表事件不得入選。

### 地圖欄

- 只有事件資料的 `map.assets` 有合格自製定位圖時才輸出。
- 每張地圖使用 Markdown 圖片附件，下一段緊接新聞式圖說。
- 地圖不屬於圖片，不計入圖片 1 至 5 張上限。
- 不得把官方震央圖、路徑圖、雨量圖或其他專業資訊圖塞進地圖欄。
- 沒有合格定位圖時，整個地圖欄省略，不顯示失敗說明。

### 圖片欄

- 只有事件資料的 `images.assets` 有合格附件時才輸出。
- 依事件資料中的既定順序完整呈現，不得因後段排版而漏掉已驗收圖片。
- 每張圖片必須以 Markdown 圖片附件與下一段圖說成對出現。
- 多張圖片直向排列，不使用圖廊、小卡或重疊排版。
- 沒有合格圖片時，整個圖片欄省略，不顯示下載、截圖、重試或失敗等後台文字。

### 欄位與事件間距

- 時間、來源、地圖、圖片、事件細節、各方說法、分析之間各空一行。
- 除地圖與圖片附件外，欄位名稱與內容不得拆成兩段。
- 第一則事件前不加分隔線。
- 兩則事件之間固定使用：

```markdown
**分析：**上一則事件分析。

---

### TWN-02. 下一則事件 - B
```

- 最後一則事件後不加多餘分隔線。

## 後續觀察

只列需要追蹤的具體條件，沿用事件編號：

```markdown
## 後續觀察

- TWN-01：追蹤官方是否更新影響範圍、數字或處置進度。
- GLB-02：若事件跨境擴散或新增獨立來源，更新各方說法與風險判斷。
```

不得為每則事件硬湊觀察項目，也不得在此重複整段詳報。

## 讀者版禁止內容

- 英文欄位名稱或未翻譯的通行外文專有名詞。
- 事件分析卡、事件資料格式、技能名稱或欄位所有權。
- 「已下載」「視覺驗收成功」「強制嘗試」「來源無圖」等後台說明。
- 只有圖說沒有附件、只有附件沒有圖說，或以網址代替可直接顯示的圖片。
- 搜尋結果頁、登入頁、錯誤頁、空白圖及未載入地圖。
- 範例檔中的示範事件。

## 送出前驗證

完成草稿後執行：

```bash
python3 scripts/validate_news_brief.py brief \
  --manifest /path/to/news-event-manifest.json \
  --input /path/to/news-brief.md
```

驗證失敗時只修正報錯欄位，不得重新生成整份簡報。

