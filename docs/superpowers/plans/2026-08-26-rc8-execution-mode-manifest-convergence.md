# rc.8 Execution-Mode and Manifest Convergence Plan

1. Add focused contract regressions for execution-mode routing, mobile audit-based Reader conservation, transport-only native image cards, and policy-stage legal-basis requirements.
2. Run the focused tests and preserve the expected failures.
3. Update existing authority documents and the candidate-audit validator only; keep the manifest schema and Reader validator strict.
4. Synchronize image-policy and README prose, then add a bilingual rc.8 version record.
5. Run focused tests, the full repository suite, capsule rebuild/verification, and two independent unchanged-fingerprint audits.
6. Commit the source and generated capsule, update GitHub `main`, and verify remote CI.
